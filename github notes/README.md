## GIT AND GITHUB

GIT - Version control system.

git means version contrloing the tool

git is tool that version control the tool and who is the author and time all wil recored.

git is a version control system that is used in industry that mantians the source code and records of your code and its version. 

github it is platform and have gui and we can perform ci/cd pipeines

git it is version control system, we wil perform in cli and distrited versioning.

In git after deliting the file. we can revoke the file. but in file system we can't recover



un-tracked -- staged  -- tracked 

## BASIC GIT COMMANDS

git-init - Create an empty Git repository or reinitialize an existing one

git status -  

git add filename - it will take untracked to stage

git commit -m "added python files" - for commiting the files 

git restore filename - after deleting the file we can also restore 

 git remote -v

git remote set-url origin 


git push -u origin main


